import { SDK, Integration, SDKResponse, ServerOptions } from "@commercetools/frontend-sdk";


function getLifetimeOrders(params) {
    const [ orders, setOrders ] = useState([]);
    const [ loading, setLoading ] = useState(false);
    const [ error, setError ] = useState(null);


    return null;
};

function getServiceByReference(params) {
    const [ service, setService ] = useState([]);
    const [ loading, setLoading ] = useState(false);
    const [ error, setError ] = useState(null);


    return null;
};